# Define a string
my_string = "Hello, World!"

# Initialize index
index = 0

# Loop through the string using a while loop
while index < len(my_string):
    # Access and print the character at the current index
    print(my_string[index])
    # Increment index to move to the next character
    index += 1
